app = "hood-conflict-bot"
primary_region = "ams"

[build]
  builder = "paketobuildpacks/builder:base"

[processes]
  app = "python main.py"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"
